// var table = +prompt("Enter Youe Table")
// var tablelenth = +prompt("Enter Your Table Lenth")

// for(var i = 1; i <= tablelenth; i++){
//     console.log(`${table} X ${i} = ${table * i}`);
// }

// Nested loop

var Fullname =["Ahsen","Shayan","Obaid"]
var lastname =["Rehan","Adeel"]
                              
for(var i = 0; i <= Fullname.length; i++){
            
    for(var j = 0; j <= lastname.length; j++){
        console.log(`${Fullname}  ${i}  ${lastname}  ${j}`);   
   }
}





// ✅ Tumhari Samajh (Corrected & Clear)

// Outer loop ki condition true hoti hai →
// program inner loop mein chala jata hai →
// inner loop poora complete hota hai →
// inner loop false hota hai →
// control wapas outer loop ke paas aata hai →
// outer loop next iteration ke liye check hota hai →
// jab tak outer loop false nahi hota, process repeat hota rehta hai.

// 💯 Bilkul sahi!

// 🔁 Execution Flow (Simple Steps)

// Outer loop condition check

// Agar true → andar jao

// Inner loop start

// Inner loop apni condition ke sath poora chalta hai

// Inner loop end

// Jab condition false hoti hai

// Control bahar nikal jata hai

// Back to outer loop

// Outer loop ka next step hota hai

// Phir se condition check hoti hai

// Repeat

// Jab tak outer loop false na ho






